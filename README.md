# mobx learn

## 环境配置

```bash
npm i -g create-react-app

create-react-app tinylog-ui --scripts-version=react-scripts-ts

cd tinylog-ui/

npm start

npm run eject # 最后一个命令使用eject将所有内建的配置暴露出来

npm i mobx react-mobx mobx-react-router -S
```